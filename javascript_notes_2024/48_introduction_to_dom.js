
    console.dir(document); // shows us all the properties methods attached to the document
    console.log(document.domain); // use . to access any of these upper properties or method
    console.log(document.URL);
    console.log(document.title);
    document.title="Document Object Model"; // to set/change them using script
    console.log(document.doctype);
    console.log(document.head);
    console.log(document.body);
    console.log(document.all); // gives us an array or an html collection of every thing inside the DOM
